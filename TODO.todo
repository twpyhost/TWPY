- [HECHO] La actividad reciente en admin dashboard debe mostrar qué se hizo y con cuales cuentas
- [HECHO] Confirmar fusión debe mostrar un modal advirtiendo que la fusión es irreversible (si asi lo es, claude avisame)
  -> Ya lo hacía (ribbon + texto + checkbox obligatorio) y sí, es irreversible (fusionar está excluido a
     propósito de los tipos deshacibles). Se completó el warningText para decir explícitamente que el
     jugador duplicado no se borra (queda oculto, marcado como fusionado) y que no hay undo desde el panel.
- [HECHO] El sidebar de admin dashboard no debe hacer blink cuando cambia de pagina dentro de la misma dashboard, solamente el contenido a la derecha
- [HECHO] Crear diseño para la configuración de puntajes y posiciones en la pagina rankings en admin dashboard (crear nueva posicion con su puntaje)
- Añadir OAUTH con Discord
  -> El código ya está completo (botón Discord en /auth/login + /auth/callback con gate por is_admin()).
     Falta: habilitar el provider en el dashboard de Supabase (redirect URLs de prod/local) y decidir cómo
     se le da el rol admin al auth.users nuevo que crea Discord (no comparte UUID con la cuenta email/password).
     Pendiente de que lo configures vos en Supabase.
- Añadir links de las redes sociales
  -> El footer ya tiene los 4 íconos (Discord/Instagram/X/Facebook) con href="#". Falta que me pases las
     URLs reales para cablearlas (y si querés sumar TikTok/YouTube/Twitch hay que dibujar el SVG, no existe en el repo).
- [HECHO] Cambiar la palabra login por blackhand en navbar
- [HECHO] src/app/api/previsualizar_torneo/route.js -- confirmado sin consumidor, borrado.

v1.1
añadir soporte a la focus league, la primera fase de grupos con vista de grupos y sus miembros, puntajes en grupos, fechas de peleas de los participantes dentro de los grupos, y luego la segunda fase como un torneo challonge normal